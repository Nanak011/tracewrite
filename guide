All members:
clone the repository git clone 
edit the env file- add .env file to backend 
cd frontend || npm install
cd backend || npm install

For backend:

cd backend
npm install nodemailer bcryptjs sanitize-html pdfkit express-session dotenv
npm install docx
npm install mammoth

cd nlp
pip install python-docx pypdf spacy nltk
python -m spacy download en_core_web_sm
